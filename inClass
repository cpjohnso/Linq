//last_firstJohn
            List<string> theJohns = MANCourse.Students
                                        .Where(s => s.FirstName == "John")
                                        .Select(s => s.LastName)
                                        .ToList();
